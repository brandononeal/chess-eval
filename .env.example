# Copy to .env.local and fill in your Chess.com username.
CHESS_USERNAME=your_chesscom_username

# Postgres connection. Matches docker-compose.yml; `docker compose up -d`
# then run `npm run db:migrate`.
DATABASE_URL=postgres://chess:chess@localhost:5432/chess_eval
